# ==========================================
# SellerAgent — 销售智能体依赖
# ==========================================

# LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# Milvus 向量数据库 (Lite 版，无需 Docker)
pymilvus>=2.4.0
milvus-lite>=2.4.0

# Embedding 模型
sentence-transformers>=3.0.0

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# BM25 关键词检索（Milvus 原生 hybrid_search 不可用时的回退方案）
rank-bm25>=0.2.2

# 工具
python-dotenv>=1.0.0
pydantic>=2.0.0

# 文档解析（Docling — 统一处理 PDF/DOCX/XLSX/PPTX/HTML/图片）
docling>=2.0.0
python-multipart>=0.0.9
